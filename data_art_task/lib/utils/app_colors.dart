import 'package:flutter/material.dart';

class AppColors {
  // Main Colors.
  static const transparent = Colors.transparent;
  static const primary = Colors.teal;
  static const secondary = Color.fromRGBO(0, 0, 0, 0.541);
  static const background = Color.fromRGBO(220, 220, 223, 1);
  static const backgroundDim = Colors.blueGrey;
  static const positive = Colors.green;
  static const negative = Color.fromRGBO(161, 39, 39, 1);

  // Text Colors.
  static const primaryTextColor = Colors.black87;
  static const secondaryTextColor = Colors.black12;

  AppColors._();
}
